import React from "react";

type PropTypes = {
  children: JSX.Element | JSX.Element[];
};

const ClubCardOutline = ({ children }: PropTypes) => {
  return <section className="p-5"></section>;
};

export default ClubCardOutline;
